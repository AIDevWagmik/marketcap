<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Buy $MCAP</title>
  <meta name="theme-color" content="#2877cd" />
  <link rel="icon" href="buy.png">
  <style>
    :root{ --bg:#bde9ff; --blue:#2877cd; }
    html,body{height:100%;margin:0}
    body{
      display:grid; place-items:center; gap:1rem;
      background:var(--bg); font-family:system-ui,Arial,sans-serif; text-align:center;
    }
    .cta{
      display:inline-flex; flex-direction:column; align-items:center; gap:.6rem;
      text-decoration:none; color:#fff; font-weight:700;
    }
    .cta img{
      width:min(65vw,280px); height:auto; border-radius:20px; background:#fff;
      box-shadow:0 10px 28px rgba(0,0,0,.25);
      transition:transform .15s ease, box-shadow .15s ease;
    }
    .cta img:hover{ transform:translateY(-2px); box-shadow:0 16px 40px rgba(0,0,0,.3); }
    .pill{ background:var(--blue); padding:.5rem .9rem; border-radius:9999px; }
  </style>

  <!-- Jupiter plugin (lazy init on click) -->
  <script src="https://plugin.jup.ag/plugin-v1.js" defer></script>
{
  "name": "Buy MCAP",
  "short_name": "MCAP Buy",
  "start_url": "/app.html",
  "display": "standalone",
  "background_color": "#bde9ff",
  "theme_color": "#2877cd",
  "icons": [
    { "src": "buy-192.png", "sizes": "192x192", "type": "image/png", "purpose": "any maskable" },
    { "src": "buy-512.png", "sizes": "512x512", "type": "image/png", "purpose": "any maskable" }
  ]
}
